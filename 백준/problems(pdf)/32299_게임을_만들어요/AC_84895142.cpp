#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef long double ld;
typedef pair<ld, ld> pld;

int main(void) {
	ios::sync_with_stdio(false);
	cin.tie(0);
	ll n = 0;
	cin >> n;
	if (n == 3) {
		cout << "Hobanwoo";
	}
	else {
		cout << "Sangho";
	}
	return 0;
}