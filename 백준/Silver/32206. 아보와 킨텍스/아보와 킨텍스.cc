#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef pair<ll, ll> pll;
map<string, ll>Map;
int main(void) {
	ios::sync_with_stdio(false);
	cin.tie(0);
	ll n = 0;
	cin >> n;
	string str = "";
	cin >> str;
	cout << 26 + 25 * str.length();
	return 0;
}