#include <bits/stdc++.h>
using namespace std;
int main(void)
{
	ios::sync_with_stdio(false);
	cin.tie(0);
	int n = 0;
	cin >> n;
	for (int i = 0; i < n; i++)
	{
		int m = 0;
		cin >> m;
		for (int j = 0; j < m; j++)
		{
			cout << '=';
		}
		cout << '\n';
	}
}